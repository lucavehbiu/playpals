# ==============================================================================
# ENVIRONMENT VARIABLES FOR LOCAL DEVELOPMENT AND HEROKU DEPLOYMENT
# ==============================================================================

# 1. APPLICATION CONFIGURATION
# ------------------------------------------------------------------------------
# Set the port the server will listen on. Heroku dynamically assigns a port,
# so the application must use the PORT environment variable in production.
# For local development, 5000 is used as a default, but it's best to use PORT.
PORT=5000

# Set the environment to production for Heroku deployment.
# For local development, you can use "development" or "production"
NODE_ENV=development

# 2. DATABASE CONFIGURATION (PostgreSQL)
# ------------------------------------------------------------------------------
# This application uses Neon Serverless Postgres via Drizzle ORM.
# You will need to create a new Neon project and get the connection string.
# Heroku also provides a PostgreSQL add-on (e.g., Heroku Postgres).
# The application is configured to use the DATABASE_URL variable, which is
# the standard Heroku convention for a database connection string.
#
# NOTE: The provided code uses the 'postgres' client library, which is compatible
# with both standard PostgreSQL and Neon.
DATABASE_URL="postgres://user:password@host:port/database"

# 3. AUTHENTICATION CONFIGURATION (Auth.js/NextAuth)
# ------------------------------------------------------------------------------
# The application uses Auth.js (@auth/express) for authentication, which requires
# a secret for signing session cookies.
# Generate a secure secret key (e.g., using 'openssl rand -base64 32').
AUTH_SECRET="your_strong_auth_secret"

# The application likely uses Google and Apple providers (based on dependencies).
# You will need to register your application with these providers to get the credentials.
#
# Google OAuth
GOOGLE_CLIENT_ID="your_google_client_id"
GOOGLE_CLIENT_SECRET="your_google_client_secret"
#
# Apple OAuth
APPLE_CLIENT_ID="your_apple_client_id"
APPLE_CLIENT_SECRET="your_apple_client_secret"

# 4. OTHER SERVICES
# ------------------------------------------------------------------------------
# The application uses OpenAI (based on dependencies).
OPENAI_API_KEY="your_openai_api_key"

# 5. REPLIT-SPECIFIC VARIABLES (Optional - for reference)
# ------------------------------------------------------------------------------
# REPL_ID=
# REPL_SLUG=
# REPL_OWNER=
# REPLIT_DB_URL=
# REPLIT_DB_READ_URL=
# REPLIT_DB_WRITE_URL=

